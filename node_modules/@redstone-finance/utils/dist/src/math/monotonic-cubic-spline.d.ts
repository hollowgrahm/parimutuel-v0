export declare class CubicInterpolation {
    private xs;
    private ys;
    private firstSlope;
    private lastSlope;
    private fun;
    constructor(xs: number[], ys: number[], firstSlope: number, lastSlope: number, fun: (x: number) => number);
    forX(x: number): number;
    forY(y: number, precision: number): number;
}
export declare const monotoneCubicInterpolation: (xs: number[], ys: number[]) => CubicInterpolation;
//# sourceMappingURL=monotonic-cubic-spline.d.ts.map